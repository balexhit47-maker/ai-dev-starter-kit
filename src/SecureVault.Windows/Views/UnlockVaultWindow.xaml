<Window x:Class="SecureVault.Windows.Views.UnlockVaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Разблокировать сейф" Height="340" Width="420" MinHeight="280" WindowStartupLocation="CenterOwner">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24">
            <TextBlock x:Name="VaultNameText" Style="{StaticResource SectionHeadingStyle}" Margin="0,0,0,14"/>

            <TextBlock Text="Мастер-пароль" Style="{StaticResource FieldLabelStyle}"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,14"/>

            <TextBlock Text="Файл-ключ (укажите, только если он использовался при создании сейфа)" Style="{StaticResource FieldLabelStyle}"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                <Button Content="Выбрать файл..." Click="OnBrowseKeyfileClick"/>
                <TextBlock x:Name="KeyfileNameText" Text="файл не выбран" Style="{StaticResource HintTextStyle}" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Margin="0,0,8,0" Click="OnCancelClick"/>
                <Button Content="Открыть" Style="{StaticResource AccentButtonStyle}" Click="OnUnlockClick"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
