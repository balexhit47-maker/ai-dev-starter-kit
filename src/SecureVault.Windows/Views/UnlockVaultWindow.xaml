<Window x:Class="SecureVault.Windows.Views.UnlockVaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Разблокировать сейф" Height="260" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock x:Name="VaultNameText" FontWeight="Bold" Margin="0,0,0,12"/>

        <TextBlock Text="Мастер-пароль" Margin="0,0,0,4"/>
        <PasswordBox x:Name="PasswordBox" Margin="0,0,0,12"/>

        <TextBlock Text="Файл-ключ" Margin="0,0,0,4"/>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="Выбрать файл..." Padding="8,4" Click="OnBrowseKeyfileClick"/>
            <TextBlock x:Name="KeyfileNameText" Text="файл не выбран" VerticalAlignment="Center" Margin="8,0,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Padding="10,4" Margin="0,0,8,0" Click="OnCancelClick"/>
            <Button Content="Открыть" Padding="10,4" Click="OnUnlockClick"/>
        </StackPanel>
    </StackPanel>
</Window>
