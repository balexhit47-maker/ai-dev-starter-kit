<Window x:Class="SecureVault.Windows.Views.MainVaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="cryptoAll" Icon="/Assets/app.ico" Height="560" Width="680" MinHeight="400" MinWidth="480" WindowStartupLocation="CenterScreen"
        Closing="OnClosing" StateChanged="OnStateChanged">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" Style="{StaticResource PageHeadingStyle}"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <TextBox x:Name="SearchTextBox" Padding="8,7,32,7"
                     Tag="Поиск по названию, тегам и содержимому..."
                     TextChanged="OnSearchTextChanged"
                     ToolTip="Поиск по названию, тегам и содержимому записей"/>
            <Button x:Name="ClearSearchButton" Content="✕" Style="{StaticResource IconButtonStyle}"
                    HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,6,0"
                    Visibility="Collapsed" Click="OnClearSearchClick" ToolTip="Очистить поиск"/>
        </Grid>

        <ListView x:Name="EntriesListView" Grid.Row="2"
                  MouseDoubleClick="OnEntriesListViewMouseDoubleClick"
                  GridViewColumnHeader.Click="OnColumnHeaderClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn x:Name="TitleColumn" Header="Название" Width="220" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn x:Name="TypeColumn" Header="Тип" Width="90" DisplayMemberBinding="{Binding Type}"/>
                    <GridViewColumn Header="Теги" Width="140" DisplayMemberBinding="{Binding TagsDisplay}"/>
                    <GridViewColumn x:Name="ModifiedColumn" Header="Изменено" Width="150"
                                    DisplayMemberBinding="{Binding ModifiedAt.LocalDateTime, StringFormat={}{0:dd.MM.yyyy HH:mm}}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Удалить" Style="{StaticResource DangerButtonStyle}" Margin="0,0,8,0" Click="OnDeleteClick"/>
            <Button Content="Открыть" Margin="0,0,8,0" Click="OnOpenClick"/>
            <Button Content="Добавить..." Style="{StaticResource AccentButtonStyle}" Click="OnAddClick"/>
        </StackPanel>
    </Grid>
</Window>
